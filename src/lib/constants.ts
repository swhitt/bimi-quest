/**
 * Minimum notability score (1-10) required to trigger Discord notifications
 * and to display the "Notable" badge in Discord embeds. Brands scoring below
 * this threshold are still scored and stored but do not generate alerts.
 */
export const NOTABILITY_NOTIFICATION_THRESHOLD = 5;
